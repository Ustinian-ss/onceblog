<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="theme-color" content="#f8f7f4">
  <script>
    (function(){
      // theme-color meta 必须在脚本之前，首屏暗色才能即时更新地址栏颜色
      var t=null;
      try{t=localStorage.getItem('theme')}catch(e){} // 隐私模式/禁 cookie 下不致中断
      var d=document.documentElement;
      if(t==='dark'||(!t&&window.matchMedia('(prefers-color-scheme:dark)').matches)){
        d.setAttribute('data-theme','dark');
        try{document.querySelector('meta[name="theme-color"]').setAttribute('content','#0b1120')}catch(e){}
      }else{
        try{document.querySelector('meta[name="theme-color"]').setAttribute('content','#f8f7f4')}catch(e){}
      }
    })()
  </script>
  <%
  // 每页独立 description：文章页优先用摘要/正文开头，其余页面用全站描述
  var page_desc = config.description;
  if (page.description){
    page_desc = page.description;
  } else if (page.excerpt){
    page_desc = strip_html(page.excerpt).replace(/s+/g, ' ').trim();
  } else if (page.content){
    page_desc = strip_html(page.content).replace(/s+/g, ' ').trim();
  }
  if (page_desc && page_desc.length > 160){ page_desc = page_desc.substring(0, 157) + '...'; }
  %>
  <meta name="description" content="<%= page_desc %>">
  <%- partial('google-analytics') %>
  <%
  var title = page.title;

  if (is_archive()){
    title = __('archive_a');

    if (is_month()){
      title += ': ' + page.year + '/' + page.month;
    } else if (is_year()){
      title += ': ' + page.year;
    }
  } else if (is_category()){
    title = __('category') + ': ' + page.category;
  } else if (is_tag()){
    title = __('tag') + ': ' + page.tag;
  }
  %>
  <title><% if (title){ %><%= title %> | <% } %><%= config.title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <%- open_graph({twitter_id: theme.twitter, fb_admins: theme.fb_admins, fb_app_id: theme.fb_app_id}) %>
  <% if (config.feed) { %>
    <%- feed_tag() %>
  <% } else if (theme.rss) { %>
    <%- feed_tag(theme.rss) %>
  <% } %>
  <% if (theme.favicon){ %>
    <%- favicon_tag(theme.favicon) %>
  <% } %>
  <% if (config.highlight.enable){ %>
    <%- css('https://cdn.jsdelivr.net/npm/typeface-source-code-pro@0.0.71/index.min.css') %>
  <% } %>
  <%- css('css/style') %>
  <% if (theme.fancybox){ %>
    <%- css('fancybox/jquery.fancybox.min.css') %>
  <% } %>
  <link rel="preconnect" href="https://cdn.jsdelivr.net">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.7.2/css/all.min.css">
  <style id="loading-style">
    #loading-box{position:fixed;z-index:99999;inset:0;display:flex;align-items:center;justify-content:center}
    #loading-box .loading-left-bg,#loading-box .loading-right-bg{position:fixed;top:0;width:50%;height:100%;background:var(--bg);z-index:99998;transition:transform .6s ease-in-out}
    #loading-box .loading-left-bg{left:0}
    #loading-box .loading-right-bg{right:0}
    #loading-box.loaded .loading-left-bg{transform:translateX(-100%)}
    #loading-box.loaded .loading-right-bg{transform:translateX(100%)}
    .spinner-box{display:flex;flex-direction:column;align-items:center;gap:18px;z-index:99999}
    .configure-core{width:100%;height:100%;background:var(--accent);border-radius:50%}
    .configure-border-1{width:44px;height:44px;padding:3px;border:3px solid transparent;border-top-color:var(--accent);border-bottom-color:var(--accent);border-radius:50%;animation:spin-clockwise 1.2s linear infinite}
    .configure-border-2{width:44px;height:44px;padding:3px;left:-44px;border:3px solid transparent;border-left-color:var(--accent-hover);border-right-color:var(--accent-hover);border-radius:50%;animation:spin-anticlockwise .8s linear infinite}
    @keyframes spin-clockwise{to{transform:rotate(360deg)}}
    @keyframes spin-anticlockwise{to{transform:rotate(-360deg)}}
    .loading-word{color:var(--text-secondary);font-size:14px;font-weight:600;letter-spacing:.08em}
    #progress-bar{position:fixed;top:0;left:0;z-index:9999;width:0;height:3px;background:linear-gradient(90deg,var(--accent),var(--accent-hover));transition:width .12s ease-out,opacity .3s ease;opacity:0}
    #progress-bar.visible{opacity:1}
  </style>
</head>
